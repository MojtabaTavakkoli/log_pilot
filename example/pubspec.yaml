name: log_pilot_example
description: Example app demonstrating all LogPilot features.
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  bloc: ^9.2.0
  log_pilot:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  log_pilot_mcp:
    path: ../../log_pilot_mcp

flutter:
  uses-material-design: true

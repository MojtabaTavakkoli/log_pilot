/// LogPilot GraphQL link.
///
/// Import this alongside `package:log_pilot/log_pilot.dart` only if your project
/// uses `graphql` / `graphql_flutter`.
library;

export 'package:log_pilot/src/network/log_pilot_graphql_link.dart';
